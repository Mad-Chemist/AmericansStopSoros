# Scraping CSV data from States

In order to run the Python script, you must have virtualenv
installed. From this directory run

```
virtualenv venv
. venv/bin/activate
pip install -r requirements.txt
python _scrape_offices.py
```
